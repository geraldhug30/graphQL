// named export has a name, have many as you needed
// default export has no name, you can only have one
const message = "I'm a programmer and it wil be";

const name = 'Gerald';

const location = 'Angeles City';

export { message, name, location as default };
